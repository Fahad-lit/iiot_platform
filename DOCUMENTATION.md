# توثيق منصة إنترنت الأشياء الصناعية والذكاء الاصطناعي\n# Industrial IoT Platform with AI/ML - Complete Documentation\n\n## 📋 جدول المحتويات\n\n1. [نظرة عامة](#نظرة-عامة)\n2. [البنية المعمارية](#البنية-المعمارية)\n3. [التثبيت والإعداد](#التثبيت-والإعداد)\n4. [واجهات برمجية التطبيقات (APIs)](#واجهات-برمجية-التطبيقات)\n5. [قواعد البيانات](#قواعد-البيانات)\n6. [الذكاء الاصطناعي والتعلم الآلي](#الذكاء-الاصطناعي-والتعلم-الآلي)\n7. [الواجهة الأمامية](#الواجهة-الأمامية)\n8. [أمثلة الاستخدام](#أمثلة-الاستخدام)\n\n---\n\n## نظرة عامة\n\nمنصة صناعية متكاملة لإدارة أجهزة إنترنت الأشياء (IoT)، جمع البيانات في الوقت الفعلي، والتحليل باستخدام الذكاء الاصطناعي والتعلم الآلي.\n\n### الميزات الرئيسية:\n\n- ✅ **جمع البيانات في الوقت الفعلي** من آلاف الأجهزة\n- ✅ **قواعد بيانات متعددة** (InfluxDB, PostgreSQL, MongoDB)\n- ✅ **معالجة الرسائل** عبر Apache Kafka\n- ✅ **التحليلات المتقدمة** والإحصائيات\n- ✅ **التنبؤ بالأعطال** والصيانة الوقائية\n- ✅ **كشف الشذوذ** باستخدام نماذج ML\n- ✅ **لوحة تحكم تفاعلية** مع رسوم بيانية\n- ✅ **نظام التنبيهات** والإشعارات\n\n---\n\n## البنية المعمارية\n\n### الطبقات الأربع:\n\n```\n┌─────────────────────────────────────────────────────────────┐\n│              الواجهة الأمامية (Frontend)                    │\n│              React.js + Tailwind CSS                        │\n└─────────────────────────────────────────────────────────────┘\n                            ↓\n┌─────────────────────────────────────────────────────────────┐\n│          واجهات برمجية التطبيقات (APIs)                     │\n│          FastAPI + Python                                  │\n└─────────────────────────────────────────────────────────────┘\n                            ↓\n┌─────────────────────────────────────────────────────────────┐\n│          وسيط الرسائل (Message Broker)                      │\n│          Apache Kafka                                       │\n└─────────────────────────────────────────────────────────────┘\n                            ↓\n┌──────────────────────────────────────────────────────────────┐\n│              قواعد البيانات المتعددة                         │\n│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │\n│  │ PostgreSQL   │  │  MongoDB     │  │  InfluxDB    │      │\n│  │ (Metadata)   │  │ (Logs/Docs)  │  │ (Timeseries) │      │\n│  └──────────────┘  └──────────────┘  └──────────────┘      │\n└──────────────────────────────────────────────────────────────┘\n                            ↓\n┌──────────────────────────────────────────────────────────────┐\n│         الذكاء الاصطناعي والتحليلات                        │\n│         AI/ML Models & Analytics                           │\n└──────────────────────────────────────────────────────────────┘\n```\n\n---\n\n## التثبيت والإعداد\n\n### المتطلبات الأساسية:\n\n- Docker و Docker Compose\n- Python 3.11+\n- Node.js 18+\n- Git\n\n### خطوات التثبيت:\n\n#### 1. استنساخ المشروع\n\n```bash\ngit clone https://github.com/your-repo/iiot_platform.git\ncd iiot_platform\n```\n\n#### 2. إعداد متغيرات البيئة\n\n```bash\ncp .env.example .env\n# تحرير الملف وتعديل القيم حسب الحاجة\nnano .env\n```\n\n#### 3. بدء الخدمات باستخدام Docker\n\n```bash\ndocker-compose up -d\n```\n\nهذا سيبدأ:\n- PostgreSQL على المنفذ 5432\n- MongoDB على المنفذ 27017\n- InfluxDB على المنفذ 8086\n- Apache Kafka على المنفذ 9092\n- Redis على المنفذ 6379\n\n#### 4. تثبيت متطلبات الخلفية\n\n```bash\ncd backend\npip install -r requirements.txt\n```\n\n#### 5. بدء خادم FastAPI\n\n```bash\npython main.py\n```\n\nالخادم سيبدأ على: `http://localhost:8000`\n\n#### 6. تثبيت متطلبات الواجهة الأمامية\n\n```bash\ncd frontend\nnpm install\n```\n\n#### 7. بدء خادم التطوير\n\n```bash\nnpm run dev\n```\n\nالواجهة الأمامية ستكون متاحة على: `http://localhost:3000`\n\n---\n\n## واجهات برمجية التطبيقات\n\n### 1. مسارات القياسات (Measurements)\n\n#### تسجيل قياس واحد\n\n```http\nPOST /api/measurements/\nContent-Type: application/json\n\n{\n  \"device_id\": \"sensor_001\",\n  \"measurement_type\": \"temperature\",\n  \"value\": 25.5,\n  \"unit\": \"°C\",\n  \"metadata\": {\n    \"humidity\": 60,\n    \"pressure\": 1013.25\n  }\n}\n```\n\n**الاستجابة:**\n```json\n{\n  \"success\": true,\n  \"message\": \"تم تسجيل القياس بنجاح\",\n  \"timestamp\": \"2025-10-30T12:00:00\"\n}\n```\n\n#### تسجيل مجموعة قياسات\n\n```http\nPOST /api/measurements/batch\nContent-Type: application/json\n\n{\n  \"measurements\": [\n    {\n      \"device_id\": \"sensor_001\",\n      \"measurement_type\": \"temperature\",\n      \"value\": 25.5,\n      \"unit\": \"°C\"\n    },\n    {\n      \"device_id\": \"sensor_002\",\n      \"measurement_type\": \"pressure\",\n      \"value\": 1013.25,\n      \"unit\": \"Pa\"\n    }\n  ]\n}\n```\n\n#### جلب قياسات الجهاز\n\n```http\nGET /api/measurements/{device_id}?limit=100&offset=0&measurement_type=temperature\n```\n\n### 2. مسارات الأجهزة (Devices)\n\n#### إنشاء جهاز جديد\n\n```http\nPOST /api/devices/\nContent-Type: application/json\n\n{\n  \"device_id\": \"sensor_001\",\n  \"device_name\": \"حساس درجة الحرارة - المصنع أ\",\n  \"device_type\": \"sensor\",\n  \"location\": \"قاعة الإنتاج\"\n}\n```\n\n#### جلب قائمة الأجهزة\n\n```http\nGET /api/devices/?status=active&device_type=sensor&limit=100\n```\n\n#### تحديث حالة الجهاز\n\n```http\nPATCH /api/devices/{device_id}/status?status=maintenance\n```\n\n### 3. مسارات التحليلات (Analytics)\n\n#### ملخص النظام\n\n```http\nGET /api/analytics/summary\n```\n\n**الاستجابة:**\n```json\n{\n  \"total_devices\": 50,\n  \"active_devices\": 48,\n  \"inactive_devices\": 2,\n  \"total_measurements\": 1000000,\n  \"total_alerts\": 25,\n  \"unresolved_alerts\": 3,\n  \"system_health\": 92.5,\n  \"timestamp\": \"2025-10-30T12:00:00\"\n}\n```\n\n#### إحصائيات الجهاز\n\n```http\nGET /api/analytics/devices/{device_id}/statistics?measurement_type=temperature&time_range=-24h\n```\n\n#### تحليل الاتجاهات\n\n```http\nGET /api/analytics/devices/{device_id}/trends?measurement_type=temperature&time_range=-7d\n```\n\n#### كشف الشذوذ\n\n```http\nGET /api/analytics/devices/{device_id}/anomalies?measurement_type=temperature&threshold=2.0\n```\n\n### 4. مسارات التنبؤات (Predictions)\n\n#### التنبؤ بأعطال الجهاز\n\n```http\nGET /api/predictions/failure/{device_id}?measurement_type=temperature&time_range=-30d\n```\n\n**الاستجابة:**\n```json\n{\n  \"device_id\": \"sensor_001\",\n  \"measurement_type\": \"temperature\",\n  \"failure_probability\": 0.35,\n  \"confidence\": 0.92,\n  \"days_to_failure\": 14,\n  \"risk_level\": \"medium\",\n  \"recommendations\": [\n    \"📅 جدول صيانة خلال الأسبوع القادم\",\n    \"📊 زيادة تكرار المراقبة\"\n  ],\n  \"timestamp\": \"2025-10-30T12:00:00\"\n}\n```\n\n#### التنبؤ بجدول الصيانة\n\n```http\nGET /api/predictions/maintenance/{device_id}?measurement_type=temperature\n```\n\n#### كشف الشذوذ باستخدام ML\n\n```http\nGET /api/predictions/anomalies/{device_id}?measurement_type=temperature&contamination=0.1\n```\n\n#### درجة صحة الجهاز\n\n```http\nGET /api/predictions/health-score/{device_id}?measurement_type=temperature\n```\n\n---\n\n## قواعد البيانات\n\n### PostgreSQL (البيانات الوصفية)\n\n#### الجداول الرئيسية:\n\n```sql\n-- جدول الأجهزة\nCREATE TABLE devices (\n    id SERIAL PRIMARY KEY,\n    device_id VARCHAR(255) UNIQUE NOT NULL,\n    device_name VARCHAR(255) NOT NULL,\n    device_type VARCHAR(100),\n    location VARCHAR(255),\n    status VARCHAR(50) DEFAULT 'active',\n    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,\n    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP\n);\n\n-- جدول المستخدمين\nCREATE TABLE users (\n    id SERIAL PRIMARY KEY,\n    username VARCHAR(255) UNIQUE NOT NULL,\n    email VARCHAR(255) UNIQUE NOT NULL,\n    password_hash VARCHAR(255) NOT NULL,\n    role VARCHAR(50) DEFAULT 'user',\n    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP\n);\n\n-- جدول التنبيهات\nCREATE TABLE alerts (\n    id SERIAL PRIMARY KEY,\n    device_id VARCHAR(255) NOT NULL,\n    alert_type VARCHAR(100),\n    severity VARCHAR(50),\n    message TEXT,\n    is_resolved BOOLEAN DEFAULT FALSE,\n    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,\n    resolved_at TIMESTAMP,\n    FOREIGN KEY (device_id) REFERENCES devices(device_id)\n);\n```\n\n### MongoDB (البيانات غير المهيكلة)\n\n#### المجموعات الرئيسية:\n\n```javascript\n// مجموعة سجلات الأخطاء\ndb.error_logs.insertOne({\n  device_id: \"sensor_001\",\n  error_type: \"connection_timeout\",\n  message: \"فشل الاتصال بالجهاز\",\n  timestamp: new Date(),\n  details: { /* بيانات إضافية */ }\n});\n\n// مجموعة مخرجات الذكاء الاصطناعي\ndb.ai_predictions.insertOne({\n  device_id: \"sensor_001\",\n  prediction_type: \"failure\",\n  confidence: 0.85,\n  predicted_value: 45.2,\n  created_at: new Date()\n});\n\n// مجموعة سجلات الصيانة\ndb.maintenance_logs.insertOne({\n  device_id: \"sensor_001\",\n  maintenance_type: \"preventive\",\n  performed_date: new Date(),\n  technician: \"أحمد محمد\",\n  notes: \"تم استبدال المستشعر\"\n});\n```\n\n### InfluxDB (السلاسل الزمنية)\n\n#### كتابة القياسات:\n\n```python\nfrom influxdb_client import InfluxDBClient, Point\n\nclient = InfluxDBClient(url=\"http://localhost:8086\", token=\"your_token\", org=\"iiot_org\")\nwrite_api = client.write_api()\n\npoint = Point(\"temperature\") \\\n    .tag(\"device_id\", \"sensor_001\") \\\n    .tag(\"location\", \"factory_a\") \\\n    .field(\"value\", 25.5) \\\n    .field(\"humidity\", 60)\n\nwrite_api.write(bucket=\"iiot_measurements\", record=point)\n```\n\n#### الاستعلام عن البيانات:\n\n```python\nquery_api = client.query_api()\n\nquery = '''from(bucket:\"iiot_measurements\")\n  |> range(start: -24h)\n  |> filter(fn: (r) => r._measurement == \"temperature\")\n  |> filter(fn: (r) => r.device_id == \"sensor_001\")'''\n\nresult = query_api.query(org=\"iiot_org\", query=query)\n```\n\n---\n\n## الذكاء الاصطناعي والتعلم الآلي\n\n### نماذج ML المدعومة:\n\n#### 1. التنبؤ بالأعطال (Failure Prediction)\n\n**الخوارزمية:** Regression + Probability Estimation\n\n**المؤشرات المستخدمة:**\n- اتجاه البيانات (Trend)\n- التقلب (Volatility)\n- معدل التدهور (Degradation Rate)\n\n**الصيغة:**\n```\nFailure Probability = (Trend Factor × 0.3) + (Volatility Factor × 0.3) + (Degradation Factor × 0.4)\n```\n\n#### 2. كشف الشذوذ (Anomaly Detection)\n\n**الخوارزمية:** Isolation Forest\n\n**المعاملات:**\n- contamination: معدل التلوث (افتراضي: 0.1)\n- random_state: بذرة العشوائية (افتراضي: 42)\n\n#### 3. جدول الصيانة الوقائية (Predictive Maintenance)\n\n**الحساب:**\n```\nDays to Maintenance = 30 / (Degradation Rate + 0.1) × (1 - Failure Probability)\n```\n\n### مثال على الاستخدام:\n\n```python\nfrom services.ml_service import ml_service\n\n# التنبؤ بأعطال الجهاز\nprediction = await ml_service.predict_device_failure(\n    device_id=\"sensor_001\",\n    measurement_type=\"temperature\",\n    time_range=\"-30d\"\n)\n\nprint(f\"احتمالية الفشل: {prediction['failure_probability']:.1%}\")\nprint(f\"عدد الأيام حتى الفشل: {prediction['days_to_failure']}\")\nprint(f\"التوصيات: {prediction['recommendations']}\")\n```\n\n---\n\n## الواجهة الأمامية\n\n### المكونات الرئيسية:\n\n#### 1. Dashboard (لوحة التحكم)\n- عرض إحصائيات النظام\n- رسوم بيانية تفاعلية\n- التنبيهات الأخيرة\n\n#### 2. Devices (الأجهزة)\n- قائمة الأجهزة مع التصفية\n- إنشاء وتعديل الأجهزة\n- عرض حالة كل جهاز\n\n#### 3. Analytics (التحليلات)\n- إحصائيات متقدمة\n- تحليل الاتجاهات\n- مقارنة الأجهزة\n\n#### 4. Predictions (التنبؤات)\n- التنبؤ بالأعطال\n- جدول الصيانة\n- درجة الصحة\n\n### التقنيات المستخدمة:\n\n- **React 18**: إطار العمل الأساسي\n- **React Router**: التوجيه\n- **Axios**: استدعاء APIs\n- **Recharts**: الرسوم البيانية\n- **Tailwind CSS**: التصميم\n- **Lucide React**: الأيقونات\n\n---\n\n## أمثلة الاستخدام\n\n### مثال 1: تسجيل قياسات من جهاز استشعار\n\n```python\nimport requests\nimport json\nfrom datetime import datetime\n\nAPI_URL = \"http://localhost:8000\"\n\n# تسجيل قياس واحد\nmeasurement = {\n    \"device_id\": \"sensor_001\",\n    \"measurement_type\": \"temperature\",\n    \"value\": 25.5,\n    \"unit\": \"°C\",\n    \"timestamp\": datetime.now().isoformat(),\n    \"metadata\": {\n        \"humidity\": 60,\n        \"pressure\": 1013.25\n    }\n}\n\nresponse = requests.post(\n    f\"{API_URL}/api/measurements/\",\n    json=measurement\n)\n\nprint(response.json())\n```\n\n### مثال 2: التنبؤ بأعطال الجهاز\n\n```python\nimport requests\n\ndevice_id = \"sensor_001\"\nmeasurement_type = \"temperature\"\n\nresponse = requests.get(\n    f\"http://localhost:8000/api/predictions/failure/{device_id}\",\n    params={\n        \"measurement_type\": measurement_type,\n        \"time_range\": \"-30d\"\n    }\n)\n\nprediction = response.json()\nprint(f\"احتمالية الفشل: {prediction['failure_probability']:.1%}\")\nprint(f\"مستوى الخطر: {prediction['risk_level']}\")\nprint(f\"التوصيات: {prediction['recommendations']}\")\n```\n\n### مثال 3: الحصول على إحصائيات النظام\n\n```python\nimport requests\n\nresponse = requests.get(\n    \"http://localhost:8000/api/analytics/summary\"\n)\n\nsummary = response.json()\nprint(f\"إجمالي الأجهزة: {summary['total_devices']}\")\nprint(f\"الأجهزة النشطة: {summary['active_devices']}\")\nprint(f\"صحة النظام: {summary['system_health']:.1f}%\")\n```\n\n---\n\n## استكشاف الأخطاء\n\n### المشاكل الشائعة:\n\n#### 1. فشل الاتصال بـ PostgreSQL\n\n```bash\n# التحقق من حالة الحاوية\ndocker-compose ps\n\n# عرض السجلات\ndocker-compose logs postgres\n\n# إعادة تشغيل الخدمة\ndocker-compose restart postgres\n```\n\n#### 2. عدم وصول Kafka\n\n```bash\n# التحقق من الاتصال\ntelnet localhost 9092\n\n# عرض المواضيع\ndocker exec iiot_kafka kafka-topics.sh --list --bootstrap-server localhost:9092\n```\n\n#### 3. بطء الاستعلامات\n\n```sql\n-- إنشاء فهارس\nCREATE INDEX idx_devices_status ON devices(status);\nCREATE INDEX idx_alerts_device ON alerts(device_id);\nCREATE INDEX idx_event_logs_created ON event_logs(created_at);\n```\n\n---\n\n## المراجع والموارد\n\n- [FastAPI Documentation](https://fastapi.tiangolo.com/)\n- [React Documentation](https://react.dev/)\n- [PostgreSQL Documentation](https://www.postgresql.org/docs/)\n- [MongoDB Documentation](https://docs.mongodb.com/)\n- [InfluxDB Documentation](https://docs.influxdata.com/)\n- [Apache Kafka Documentation](https://kafka.apache.org/documentation/)\n- [Scikit-learn Documentation](https://scikit-learn.org/)\n\n---\n\n**آخر تحديث:** أكتوبر 2025\n**الإصدار:** 1.0.0\n**الحالة:** قيد التطوير\n
