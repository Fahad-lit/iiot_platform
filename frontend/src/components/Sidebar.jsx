import React from 'react'\nimport { Link, useLocation } from 'react-router-dom'\nimport {\n  LayoutDashboard,\n  Cpu,\n  TrendingUp,\n  BarChart3,\n  AlertCircle,\n  Settings,\n  Activity\n} from 'lucide-react'\n\nfunction Sidebar({ isOpen }) {\n  const location = useLocation()\n\n  const menuItems = [\n    { path: '/', label: 'لوحة التحكم', icon: LayoutDashboard },\n    { path: '/devices', label: 'الأجهزة', icon: Cpu },\n    { path: '/measurements', label: 'القياسات', icon: Activity },\n    { path: '/analytics', label: 'التحليلات', icon: BarChart3 },\n    { path: '/alerts', label: 'التنبيهات', icon: AlertCircle },\n    { path: '/settings', label: 'الإعدادات', icon: Settings },\n  ]\n\n  return (\n    <aside className={`${\n      isOpen ? 'w-64' : 'w-20'\n    } bg-slate-900 text-white transition-all duration-300 overflow-y-auto`}>\n      <div className=\"p-6 border-b border-slate-700\">\n        <div className=\"flex items-center gap-3\">\n          <div className=\"w-10 h-10 bg-blue-500 rounded-lg flex items-center justify-center\">\n            <Activity size={24} />\n          </div>\n          {isOpen && <span className=\"font-bold text-lg\">IIoT</span>}\n        </div>\n      </div>\n\n      <nav className=\"p-4 space-y-2\">\n        {menuItems.map((item) => {\n          const Icon = item.icon\n          const isActive = location.pathname === item.path\n\n          return (\n            <Link\n              key={item.path}\n              to={item.path}\n              className={`flex items-center gap-3 px-4 py-3 rounded-lg transition-colors ${\n                isActive\n                  ? 'bg-blue-600 text-white'\n                  : 'text-gray-300 hover:bg-slate-800'\n              }`}\n              title={!isOpen ? item.label : ''}\n            >\n              <Icon size={20} />\n              {isOpen && <span>{item.label}</span>}\n            </Link>\n          )\n        })}\n      </nav>\n\n      <div className=\"p-4 border-t border-slate-700 mt-auto\">\n        <div className=\"bg-slate-800 rounded-lg p-4\">\n          {isOpen && (\n            <>\n              <p className=\"text-sm font-semibold mb-2\">الإصدار</p>\n              <p className=\"text-xs text-gray-400\">v1.0.0</p>\n            </>\n          )}\n        </div>\n      </div>\n    </aside>\n  )\n}\n\nexport default Sidebar\n
